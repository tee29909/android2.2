<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        tools:context=".MainActivity" android:paddingStart="@dimen/layoutpadding"
        android:paddingEnd="@dimen/layoutpadding">

    <TextView
            android:text="@string/theerapat_59160571"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" android:id="@+id/name_text"
            android:textAlignment="center"
            style="@style/namesty"/>
    <ImageView
            android:layout_width="match_parent"
            android:layout_height="wrap_content" app:srcCompat="@android:drawable/btn_star_big_on"
            android:id="@+id/star_image" android:contentDescription="@string/yellow_star"/>
    <ScrollView
            android:layout_width="match_parent"
            android:layout_height="match_parent" android:id="@+id/bioscro">
        <TextView
                android:text="@string/bio"
                android:layout_width="match_parent"
                android:layout_height="wrap_content" android:id="@+id/bio_text" style="@style/namesty"
                android:lineSpacingMultiplier="1.2"/>
    </ScrollView>

</LinearLayout>